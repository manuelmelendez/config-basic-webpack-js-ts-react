/**
 * This function is a plus for two numbers
 * @param {Number} a This is the first number
 * @param {Number} b This is the second number
 * @returns {Number} return the plus of first number + second number
 */

const sum = (a, b) => {
    return a + b;
}

export default sum;